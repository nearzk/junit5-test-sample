spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: sa
    schema: database.sql
  jpa:
    database: H2
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
